name: weatherApp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0-334.3.beta

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.1
  flutter_bloc: ^8.1.6
  weather: ^3.1.1
  equatable: ^2.0.5
  intl: ^0.19.0
  

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

